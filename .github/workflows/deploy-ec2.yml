name: Deploy, Rollback & Terminate EC2

on:
  workflow_dispatch:

jobs:
  deploy-rollback:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get short SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Prepare SSH key
        run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > flaskkey.pem
          chmod 600 flaskkey.pem

      - name: Launch EC2 instance
        id: ec2
        run: |
          INSTANCE_ID=$(aws ec2 run-instances \
            --image-id ami-03c1f788292172a4e \
            --instance-type t2.micro \
            --key-name flaskkey \
            --security-group-ids flask-app-sg \
            --tag-specifications "ResourceType=instance,Tags=[{Key=Name,Value=Flask-Rollback-Test-${{ github.run_id }}}]" \
            --query 'Instances[0].InstanceId' \
            --output text)
          echo "INSTANCE_ID=$INSTANCE_ID" >> $GITHUB_OUTPUT

      - name: Wait for instance to be running
        run: aws ec2 wait instance-running --instance-ids ${{ steps.ec2.outputs.INSTANCE_ID }}

      - name: Get public IP
        id: ip
        run: |
          PUBLIC_IP=$(aws ec2 describe-instances \
            --instance-ids ${{ steps.ec2.outputs.INSTANCE_ID }} \
            --query 'Reservations[0].Instances[0].PublicIpAddress' \
            --output text)
          echo "PUBLIC_IP=$PUBLIC_IP" >> $GITHUB_OUTPUT

      - name: Wait for SSH
        run: |
          IP=${{ steps.ip.outputs.PUBLIC_IP }}
          for i in {1..20}; do
            if nc -z $IP 22; then
              break
            fi
            sleep 5
          done

      - name: Install Docker on EC2
        run: |
          ssh -o StrictHostKeyChecking=no -i flaskkey.pem ubuntu@${{ steps.ip.outputs.PUBLIC_IP }} \
            "sudo apt update -y && sudo apt install -y docker.io && sudo usermod -aG docker ubuntu"

      - name: Copy project to EC2
        run: |
          scp -o StrictHostKeyChecking=no -i flaskkey.pem -r . ubuntu@${{ steps.ip.outputs.PUBLIC_IP }}:/home/ubuntu/app

      - name: Build and run Docker container (with version)
        run: |
          ssh -o StrictHostKeyChecking=no -i flaskkey.pem ubuntu@${{ steps.ip.outputs.PUBLIC_IP }} \
            "cd /home/ubuntu/app && \
             sudo docker build -t flask-app:${{ env.SHORT_SHA }} --build-arg VERSION=${{ env.SHORT_SHA }} . && \
             sudo docker run -d -p 80:5000 --name flask-app flask-app:${{ env.SHORT_SHA }}"

      - name: Wait 2 minutes (initial deploy live)
        run: |
          echo "✅ App rodando em http://${{ steps.ip.outputs.PUBLIC_IP }}"
          echo "Acesse http://${{ steps.ip.outputs.PUBLIC_IP }}/health para ver a versão"
          sleep 120

      - name: Stop and remove container (simulate failure)
        run: |
          ssh -o StrictHostKeyChecking=no -i flaskkey.pem ubuntu@${{ steps.ip.outputs.PUBLIC_IP }} \
            "sudo docker stop flask-app && sudo docker rm flask-app"
          echo "🛑 Container removido"

      - name: Rollback restart same version
        run: |
          ssh -o StrictHostKeyChecking=no -i flaskkey.pem ubuntu@${{ steps.ip.outputs.PUBLIC_IP }} \
            "sudo docker run -d -p 80:5000 --name flask-app flask-app:${{ env.SHORT_SHA }}"
          echo "🔁 Rollback concluído para versão: ${{ env.SHORT_SHA }}"

      - name: Wait 3 minutes (post-rollback)
        run: |
          echo "Agora teste novamente o /health — deve mostrar a mesma versão."
          sleep 180

      - name: Terminate EC2 instance
        if: always()
        run: |
          aws ec2 terminate-instances --instance-ids ${{ steps.ec2.outputs.INSTANCE_ID }}
          echo "Instância ${{ steps.ec2.outputs.INSTANCE_ID }} terminada."
